# Note: Stroll.TestMcp directory has some locked log files that couldn't be removed

The directory contains only build artifacts and log files that are
locked by the system. It can be safely deleted when the file locks are released.
